# SECURITY.md — Kravn

> **Read this before touching auth, tokens, routes, MCP, OAuth, SSO, plugins, DB access, headers or
> secrets — and before declaring ANY change "done".** This is the security contract for Kravn: the
> invariants that must never be broken, the controls already in place, the accepted residual risk, and the
> **mandatory re-validation process** every change goes through. `AGENTS.md` §5 points here on purpose so
> it is always in context.

Kravn is an MCP **gateway / registry / proxy** exposed publicly (reference deployment: `mcp.worldsys.io`).
It brokers untrusted external MCP clients (e.g. Claude connectors) to configured upstream MCP servers, runs
an OAuth 2.1 authorization server, and executes admin-provided code (native + user plugins, stdio servers).
The attack surface is real and the product **must be secure on its own** — a CDN/WAF (Cloudflare) is
optional hardening, **never** a control we depend on.

---

## 0. The one rule

**Every change re-validates security.** Not "security-looking" changes — every change. Small diffs move
auth boundaries too. Before you call work done, run the checklist in §6 against your diff; if the change
touches a security-sensitive surface (§5), run an adversarial review and fix confirmed findings first.
"It builds and the happy path works" is not done.

---

## 1. Trust boundaries & threat model

| Boundary | Who's on the other side | Primary token | Enforcement point |
|---|---|---|---|
| **Control plane** (`/api/*`, operator SPA) | Authenticated staff (admin/editor/viewer) | Unscoped session JWT (Bearer) | `authenticate` + `authorize(<perm>)` in `auth/plugin.ts` |
| **Data plane** (`/mcp`, `/servers/:slug/mcp`) | Signed-in users AND external OAuth clients | Session JWT **or** `scope='mcp'` JWT | `mcp.routes.ts` (auth + `mcp.invoke` + server `access` policy) |
| **OAuth 2.1 AS** (`/oauth/*`, `/.well-known/*`) | Untrusted third-party MCP clients | DCR client creds, auth codes, refresh tokens | `oauth.routes.ts` + `oauth.service.ts` |
| **SSO/SAML/OIDC** (`/api/auth/sso/*`) | External IdP (EntraID, etc.) | Signed SAML assertion / OIDC id_token | `sso.service.ts` (signature + issuer checks) |
| **Upstream MCP servers** | Third-party/self-hosted MCP endpoints | Per-server config (secrets encrypted) | `mcp/upstream.ts` |
| **Plugins & stdio servers** | **Admin-authored code, runs in-process / as child** | — (trusted-admin surface) | `servers.routes.ts` gating + `upstream.ts` env sanitization |

**Untrusted inputs:** request bodies/params/headers, SSO responses, OAuth params, upstream MCP tool output.
**Trusted-but-dangerous:** user plugins (run in-process with full privilege) and stdio servers (arbitrary
`command`/`args` → RCE). These are **admin-only by design**; the boundary is "only an admin can introduce
executable code", enforced in code — not "the code is sandboxed" (it isn't).

**Token scope model (never blur these):**
- **unscoped** → full control-plane session. `authenticate` accepts ONLY this.
- **`mcp`** → data-plane only (OAuth-issued, for MCP invocation). Rejected on the control plane.
- **`handoff`** → one-time SSO→session exchange code (2-min TTL, atomic single-use).
- **`logstream`** → one-time SSE ticket for `/api/logs/stream` (1-min TTL, atomic single-use).

---

## 2. Security invariants (must never be violated)

1. **Auth tokens are Bearer-only and never travel in a URL.** SSE/redirect flows use short-lived,
   purpose-scoped, **atomically single-use** tickets/codes (`consume()` in `repos.ts`, backed by the
   `token_revocations.jti` PRIMARY KEY). Single-use enforcement is **atomic and fail-closed** — never
   check-then-act, never swallow the duplicate-key error.
2. **Scope confinement.** The control plane (`authenticate`) rejects **any** scoped token. Scoped tokens
   reach only their one surface.
3. **Authorize + own.** Every mutating route sits behind `authenticate` + `authorize(<permission>)`. Every
   per-user / per-team read additionally enforces **ownership/membership** — a permission is not authority
   over *someone else's* resource (IDOR). Multi-tenant data paths are scoped by `user_id` / team.
4. **No secret paired with a caller-supplied URL. No redirect origin derived from request headers.**
   Redirect targets come from server config (`KRAVN_PUBLIC_URL` / `KRAVN_CLIENT_URL`), never from `Host`
   or `X-Forwarded-*`.
5. **stdio/plugins are admin-only.** Creating/patching/syncing a `stdio` server requires `role==='admin'`
   **and** `KRAVN_ALLOW_STDIO`; child processes get a **sanitized env** (no secret inheritance, no
   loader-injection vars — see `DANGEROUS_ENV` in `upstream.ts`).
6. **Secrets are encrypted at rest, write-only, fail-closed.** Config fields marked `secret:true` are
   AES-256-GCM encrypted (`Encryptor`), masked in every API/UI response, decrypted only at runtime.
7. **Defense-in-depth transport controls, set in the app.** Security headers on **every** response (staged
   at `onRequest`), strict CORS allowlist, configurable `trustProxy` (never blindly `true`). The app is
   safe without a CDN.
8. **Errors don't leak.** Client mistakes → 4xx with a generic message; never a stack trace, DB error,
   schema shape (`ZodError.issues`), or internal detail. Unexpected → 500 generic.
9. **All SQL is parameterized** (`Store` `?`-placeholders via knex.raw) and **schema identifiers are
   validated** (`KRAVN_DB_SCHEMA` is a simple-identifier check). Never interpolate untrusted input into SQL
   or qualify identifiers with a caller-influenced schema.
10. **SSO signatures are verified.** SAML: `wantAssertionsSigned:true` (EntraID signs the assertion).
    OIDC discovery URLs must be **public HTTPS** (SSRF guard rejects loopback/private/link-local/metadata).

---

## 3. Controls currently in place (inventory)

| Area | Control | Where |
|---|---|---|
| Local auth | Rate-limit + progressive lockout on login; reserved-email + promotion safeguards; TOCTOU-safe delete | `auth.service.ts`, `auth.routes.ts` |
| Local auth | Disable local auth (SSO-only) + promote an EntraID user to admin | `settings` + `auth.routes.ts` |
| SSO | SAML assertion-signature verification (`wantAssertionsSigned`, `wantAuthnResponseSigned:false`), multi-cert rollover; `@fastify/formbody` for POST binding | `sso.service.ts`, `app.ts` |
| SSO | OIDC discovery **SSRF guard** (`assertPublicHttpsUrl`); reject `email_verified:false` | `sso.service.ts` |
| SSO→session | One-time **`handoff` code** in redirect (not a raw token) → `POST /api/auth/exchange`, **atomic single-use** | `sso.routes.ts`, `auth.routes.ts` |
| AuthZ | RBAC (admin `*` / editor / viewer) enforced by `authorize()`; `authenticate` rejects scoped tokens | `packages/contracts/src/permissions.ts`, `auth/plugin.ts` |
| AuthZ | **Admin-console gate**: every control-plane route (all use `authorize()`) requires role `admin` OR membership in the Platform Administrator Team. A pure MCP consumer (authenticated, not in the team) is denied the console but can still use chat/MCP. Team is seeded/reconciled with all admins; promotion joins it, demotion leaves it. Chat/MCP/OAuth-consent/`/api/auth/me` are consumer paths and intentionally NOT gated. | `auth/plugin.ts`, `db/repos.ts` (TeamsRepo), `services.ts` |
| AuthZ | Team-roster read requires membership or admin (anti-IDOR) | `teams.routes.ts` |
| MCP data-plane | `mcp.invoke` required; per-server `access` = `public` / `authenticated` / `restricted` (`allowedRoles`/`allowedTeams`) | `mcp.routes.ts`, `chat.routes.ts` |
| RCE surface | stdio create/patch/sync admin-gated + `KRAVN_ALLOW_STDIO`; child env sanitized (`DANGEROUS_ENV`) | `servers.routes.ts`, `mcp/upstream.ts` |
| Logs (SSE) | `logstream` ticket (1-min, **atomic single-use**), never a session token in `?ticket=` | `logs.routes.ts` |
| OAuth 2.1 AS | DCR (RFC 7591) rate-limited + capped; PKCE S256; **atomic** single-use auth codes & rotating hashed refresh tokens (`Store.delCount`); consent **binding cookie** (anti-fixation); `Cache-Control: no-store` | `oauth.routes.ts`, `oauth.service.ts`, `repos.ts` |
| Single-use tokens | `TokensRepo.consume(jti)` — atomic first-wins via `token_revocations` PK, fail-closed | `db/repos.ts` |
| Transport | Security headers every response (`onRequest`): CSP, HSTS, `X-Content-Type-Options`, `X-Frame-Options:DENY`, `Referrer-Policy`, `Permissions-Policy` | `app.ts` |
| Transport | CORS allowlist (only `publicUrl`/`clientUrl` origins; `credentials:false`); configurable `trustProxy` (default 1 hop, anti-XFF-forgery) | `app.ts`, `config/env.ts` |
| Secrets | AES-256-GCM `Encryptor` (`enc:v1:`), write-only masking, idempotent | `plugins/manager.ts` |
| Input | zod validation + maxlength; malformed body → 400; DB errors sanitized (`describeSyncError`) | route handlers, `app.ts` |
| Metrics | `/metrics` requires `KRAVN_METRICS_TOKEN` (or auth); 404 when disabled | `system.routes.ts` |
| Logging | Request logs strip the query string (no token/ticket leakage); secrets never logged | `app.ts` |
| DB | Parameterized SQL only; `KRAVN_DB_SCHEMA` validated; identifiers unqualified | `db/store.ts`, `db/knex.ts` |

---

## 4. Known residual risk (accepted / tracked — revisit on relevant change)

1. **Team entitlements cover MCPs + tools, but not resources/prompts (by design).** Per-team access is
   enforced at two levels: level 1 = which virtual servers a team may use (`virtual_servers.allowed_teams`,
   only when `access:'restricted'`); level 2 = which of that server's TOOLS (`team_server_tools`, empty ⇒
   all). Enforcement narrows `scope.tools` in `mcp.routes.ts` + `chat.service.ts` (covers tools/list &
   tools/call); the raw `/api/tools/:id/invoke` playground and the global `/mcp` catalog are admin-only so
   neither bypasses it; membership is loaded **live** from the DB on every request (revoking a member cuts
   an existing OAuth token's access immediately). **Limitation:** a granted team member sees ALL of that
   server's **resources and prompts** — the subset is tools-only. To scope resources/prompts, put them in a
   separate virtual server. Note also `access:'authenticated'` remains open to the whole org; use
   `restricted` (granting a team auto-flips to it) for anything sensitive. Extending level 2 to
   resources/prompts (mirror `team_server_tools`) is a tracked follow-up.
2. **HSTS / `X-Content-Type-Options` at the edge.** The app emits both; they did not surface through
   Cloudflare in a live probe. Confirm at the origin and check Cloudflare Transform/Managed-Headers rules.
   Enable "Always Use HTTPS" + HSTS at the ingress regardless (the app cannot force TLS it doesn't
   terminate).
3. **OIDC deeper SSRF.** Discovery URL is guarded; a restricted dispatcher for `openid-client`'s
   subsequent calls is a follow-up.
4. **Handoff code TTL** is a fixed 2 min (mitigated by atomic single-use). Make configurable if needed.
5. **CORS trailing-dot** normalization is cosmetic (browsers never send a trailing-dot Origin).
6. **Last-admin anti-lockout uses an in-process mutex** (`withAdminLock`) + a count check. It serializes within one gateway process but not across replicas, so two pods concurrently demoting/disabling two *different* admins could in theory drop the count to zero. Acceptable for single-replica or low-concurrency admin ops; a cross-dialect atomic guard (or a DB advisory lock) is the follow-up. Pre-dates the ABM change. Disabling an admin also leaves their Platform-Administrator-Team row (harmless — a disabled user can't authenticate, so the membership is inert).
7. **Cross-replica shared store (rate limit + OIDC login state) is best-effort.** With `KRAVN_REDIS_URL` set (the Helm chart deploys **Dragonfly**, RESP-compatible), the brute-force counters and in-flight OIDC `state` are shared across pods (`cluster/shared-store.ts`) — so login/DCR limits and OIDC callbacks are correct behind N replicas. On a store **outage** each pod transparently degrades to its own in-process counters (per-pod limiting — the single-replica guarantee, never fail-open, never a crash; an unreachable/misconfigured URL only logs and degrades). Two accepted consequences: (a) during an outage the effective login-attempt budget is per-pod × replicas until the store returns; (b) the in-memory **log ring buffer** (`logstore.ts`) remains per-pod (a UI log view shows only the serving pod's slice) — a durable shared event store is a separate follow-up. When `KRAVN_REDIS_URL` is unset (single replica) behavior is unchanged. The OIDC pending claim is atomic and single-use (`GETDEL` / memory get-and-delete), so a replayed/raced `state` cannot be reused; the PKCE verifier is stored server-side only. A brownout (store reachable but slow) is bounded by a circuit breaker (one timed-out probe per cooldown, not per request). Redis key growth under a spray is bounded by short TTLs + the per-IP login limit + the 254-char email cap; the memory fallback additionally hard-caps entry count. Two accepted follow-ups on the **bundled** Dragonfly: it has **no `requirepass`** (a same-namespace pod is fenced off by an optional NetworkPolicy, default-on, but not authenticated — use an external managed store with a `redis://:password@` URL for real auth), and it runs `noeviction` (an OOM flood rejects writes → the safe per-pod degrade, never a crash).

---

## 5. When a change is "security-sensitive" (triggers the full review)

Treat a change as security-sensitive — and run the adversarial review in §7 — if it touches **any** of:

- Authentication, sessions, JWTs, tokens, tickets, or the `consume()`/revocation path.
- Authorization: routes, `authorize()`/permissions, roles, team/ownership checks, the MCP `access` policy.
- The OAuth AS, SSO/SAML/OIDC, or any redirect/callback.
- MCP invocation, virtual-server config, upstream connection, stdio, or the plugin system.
- Any new route, or a change to CORS, security headers, `trustProxy`, error handling, or logging.
- DB access (new SQL, new columns), secret handling, or `KRAVN_DB_SCHEMA` usage.
- Anything parsing untrusted input (bodies, files, SSO/OAuth payloads, upstream tool output).

If in doubt, it's sensitive.

---

## 6. Per-change security checklist (run against your diff, every time)

- [ ] **New/changed routes** are behind `authenticate` + the correct `authorize(<perm>)`; mutating routes are not reachable by viewer/editor unless intended.
- [ ] **Ownership/membership** is enforced on every per-user/per-team read & write (not just the permission) — no IDOR by id enumeration.
- [ ] **No token in any URL**; SSE/redirect uses a scoped, **atomically** single-use ticket/code (`consume()`), fail-closed. No new `.catch(()=>{})` that swallows a uniqueness/consume failure.
- [ ] **Scope**: control-plane paths reject scoped tokens; a scoped token can't reach a new surface.
- [ ] **No secret + caller URL**; no redirect origin from `Host`/`X-Forwarded-*`.
- [ ] **stdio/plugin/exec** paths stay admin-gated; child env stays sanitized.
- [ ] **Secrets** you add are `secret:true` (encrypted, masked, never returned/logged).
- [ ] **Input** validated (zod + limits); malformed → 4xx; **errors leak nothing** (no stack/DB/schema/`ZodError.issues`).
- [ ] **SQL** parameterized; no schema/identifier interpolation; migration append-only.
- [ ] **Headers/CORS/trustProxy** unaffected or correctly updated; new response types still get security headers.
- [ ] **Runtime-validated** the specific control you touched (positive **and** negative case).
- [ ] If §5 sensitive: **adversarial review run**, confirmed findings fixed, refuted ones noted.

---

## 7. How to re-validate (the process that found and killed real bugs)

1. **Non-intrusive live probes** (authorized asset only, read-only, low volume — no brute force, no
   writes): security headers present, `/metrics` 401, CORS rejects a foreign `Origin`, protected route 401,
   discovery issuer HTTPS, malformed body 400, no version leak.
2. **Adversarial white-box review** (the pattern that surfaced the RCE, the JWT-in-URL, and the two TOCTOU
   races): fan out readers by lens (access-control / auth-changes / OAuth / MCP authz) → collect candidate
   findings → **adversarially verify each** (default to *refuted* unless a concrete exploit path is traced
   in code) → fix only what survives, at corrected severity. Do not trust a finding you haven't traced.
3. **Runtime tests** for the exact controls changed — e.g. concurrent single-use (exactly one winner),
   scope rejection (403 on control plane), non-member roster read (403), editor→stdio (403).

Findings are ranked by verified severity and fixed **before** release. Perimeter probes confirm the fix is
actually live (a git push is not a deploy).

---

## 8. Security change log (audit trail)

| Version | Security work |
|---|---|
| v0.1.11 | Local-login rate-limit + lockout hardening; SSO-only mode; EntraID admin promotion; 12 findings fixed (account-takeover-on-promotion, reserved_email, victim-lockout DoS, bounded eviction, TOCTOU delete mutex). |
| v0.1.13 | SAML signature root-cause fix (`wantAuthnResponseSigned:false`, keep `wantAssertionsSigned:true`). |
| v0.1.14 | OAuth 2.1 AS hardening: atomic single-use codes/refresh (`delCount`), consent binding cookie, DCR rate-limit + cap, refresh revoke-on-delete, `Cache-Control: no-store`. |
| v0.1.17 | Plugin secret fields encrypted at rest (idempotent, write-only masking). |
| v0.1.20 | White-box audit: editor→stdio **RCE** closed (admin-gate + `sanitizeChildEnv`); **JWT-in-URL** removed (logstream ticket + handoff exchange + scope rejection); `trustProxy` configurable; OIDC SSRF guard; `email_verified` check; security headers (`onRequest`); `/metrics` auth; CORS allowlist; 4xx passthrough; query-string stripped from logs. |
| v0.1.21 | Re-audit: **TOCTOU** in handoff exchange (**critical**) and logstream ticket (**high**) fixed with atomic `consume(jti)` (jti-PK first-wins, fail-closed); team-roster **IDOR** fixed (membership/admin check); `ZodError` detail leak removed. |
| v0.1.22 | Per-team MCP + tool entitlements (`team_server_tools`, `allowedToolIdsForUser`, enforced by narrowing `scope.tools` in mcp.routes + chat). Corollaries: `viewer` gains `mcp.invoke` (the WHICH is now decided by team policy, not role); global `/mcp` made **admin-only**; admin bypasses the restricted gate. Adversarial review found + fixed a **critical** invocation bypass — the raw `/api/tools/:id/invoke` playground ignored VS/team gating; now admin-only (UI button gated too). |
| v0.1.24 | **Admin-console gate**: access to the whole control-plane now requires role admin OR Platform Administrator Team membership (seeded at setup/promotion, reconciled at boot, removed on demotion), closing the hole where any authenticated MCP consumer could enter the admin web. Adversarial review found + fixed: `/api/overview` was `authenticate`-only (leaked dashboard) → gated; `/api/tools/:id/invoke` now also goes through `authorize()`. Refuted 3 false positives that would have broken consumers (gating `/api/oauth/consent`, `/api/auth/me`, `/api/logs/*`). Role-admin backstop eliminates the team-membership lockout class. |
| v0.1.25 | Native **Jira** plugin (`plugins/jira.ts`, credential-bearing external integration, same pattern as SharePoint: `apiToken` is `secret:true` → encrypted at rest + write-only-masked). Security review found + fixed: the `baseUrl` SSRF guard missed IPv6 literals (WHATWG normalizes `::ffff:127.0.0.1` to hex) → now rejects all IPv6 literals + loopback/link-local; added `redirect: 'error'`, a 20s timeout and a 10 MB response cap + error-message truncation. JQL/issue-keys are body/`encodeURIComponent`-safe. `baseUrl` is admin-config (behind the console gate), not caller-supplied. |
| v0.1.26 | **Confluence** plugin (shares the hardened `plugins/atlassian.ts` SSRF guard + auth with Jira). **User ABM**: edit name/email/role/password + a `disabled` flag (migration 006); `login`/`authenticate`/`authenticateToken` reject disabled, so deactivation cuts sessions live. Anti-lockout (can't demote/disable/self-disable the last admin), email uniqueness, promote↔demote syncs the Platform Administrator Team. **SCIM 2.0** provisioning (`/scim/v2/*`, `scim/scim.service.ts`): bearer token stored as a SHA-256 hash, timing-safe + enabled-gated; a MACHINE path (not behind the console gate) that provisions users at a clamped role (**never admin**) and **never deactivates/deletes an admin** (anti-lockout); admin token mgmt under `/api/scim/*` (users.write). Review found + fixed: `disabled` was not checked in the token-**issuance** paths → added to `/api/auth/exchange` and OAuth `issueTokens` (covers code + refresh); SCIM list now paginates (`startIndex`/`count` clamp); Confluence echoes a clipped CQL + encodes `pageId`. |
| v0.1.53 | **Upstream OAuth 2.1 client** — Kravn can connect to OAuth-protected remote MCP servers (Notion/Linear/Stripe/…) as an OAuth CLIENT. Protocol correctness (protected-resource + AS metadata discovery, Dynamic Client Registration, PKCE, code exchange, refresh) is delegated to the MCP SDK's audited auth toolkit; Kravn owns storage, the CSRF anchor and encryption. **The public callback `GET /oauth/upstream/callback` carries no Kravn session — its ONLY trust anchor is `state`**: `crypto.randomBytes(32)` base64url, **single-use** (atomic `delCount` claim in `takePending`), **expiring** (600s + gc), and bound to a `server_id` **server-side** — the callback query cannot choose which server gets connected (serverId comes from the pending row, not user input). The callback returns **static HTML with every reflected value (`error`/`error_description`) HTML-escaped** and **no inline script** (the global CSP `script-src 'self'` would block one anyway) — no reflected-XSS, no attacker redirect. **All token material is encrypted at rest** (migration 012 `server_oauth`): access token, refresh token, the PKCE `code_verifier` (in `server_oauth_pending`) and the registered client (incl. `client_secret`) are `Encryptor`-sealed (`enc:` prefix) and never logged; a refresh that omits a new refresh_token keeps the prior one. **SSRF:** `assertUrlAllowed` is called on the server URL and the discovered authorization-server URL, and every SDK fetch (registration/token/metadata endpoints) flows through the globally-installed SSRF dispatcher (pinned-IP lookup blocks private/metadata ranges); the server URL is operator/admin config behind the console gate, not end-user input. **RBAC:** only `servers.write` (admin/editor behind the Platform-Admin-Team gate) can initiate; tokens are never emitted on any API response (`mapServer` omits them, as with `auth_value`). `resource` (RFC 8707) binds the token audience to the MCP server URL. Validated end-to-end against a mock AS + mock MCP server (14/14): discovery, DCR, PKCE-S256 verified at the token endpoint, encrypted token storage, **single-use state (replay rejected)**, and automatic refresh on expiry. Adversarially reviewed (state/CSRF, callback XSS, SSRF, token handling, redirect-URI consistency). |
| v0.1.50 | **External key management (KMS/HSM) for at-rest encryption via envelope encryption** (bank-readiness gap #3 — key custody). The `Encryptor` is now key-set driven: it writes with an ACTIVE key and reads any format it holds a key for, dispatched by ciphertext prefix (`enc:v1:` = bootstrap-secret key, unchanged & byte-compatible; `enc:v2:` = a KMS-wrapped Data Encryption Key). A new `KeyManager` (run once at boot, async) resolves the key set: default mode derives the key from `KRAVN_SECRET`/`secret.key` exactly as before; KMS mode (`KRAVN_KMS_PROVIDER=vault|azure`) generates a random 256-bit DEK, has the external KMS **wrap** it (HashiCorp Vault Transit, or Azure Key Vault `wrapKey` RSA-OAEP-256 via Entra client-credentials), persists **only the wrapped DEK** (migration 011 `app_keyring`, one row), and **unwraps it into memory once** — the KEK never leaves the KMS/HSM, and encrypt/decrypt stay synchronous (no call-site changes). **Backward-compatible & fail-closed:** the bootstrap key is retained as a read-only fallback so pre-existing `enc:v1:` secrets keep decrypting (lazy upgrade to `enc:v2:` on next save — no bulk re-encryption); a configured-but-unreachable KMS makes the gateway **fail to boot** rather than run unprotected; the keyring records which provider wrapped the DEK and a provider swap is rejected (no confused vault↔azure unwrap). The DEK is never persisted or logged in plaintext, `unwrapKey` asserts a 32-byte key (a malformed KMS response can't inject a bad AES key), and provider HTTP is bounded (`redirect:'error'` + 10s timeout + capped response). The KMS endpoint is operator ENV config (trusted like `DATABASE_URL`, never user-supplied) so it is intentionally outside the SSRF allowlist (an internal Vault address is legitimate). Multi-replica first-boot race is handled: the PK-on-`id` insert loser re-reads and unwraps the winner's DEK so all replicas converge on one key. Validated on real sqlite + a mock Vault (16/16): env path byte-unchanged, existing `enc:v1:` still decrypts under KMS, `enc:v2:` round-trip, DEK survives a simulated restart (one wrap, re-unwrap), keyring stores only the wrapped DEK, tamper rejected. Limits documented in [`KEY_MANAGEMENT.md`](./KEY_MANAGEMENT.md): JWT signing still uses the bootstrap secret; KEK/DEK rotation + AWS/GCP KMS providers are the planned follow-ups. |
| v0.1.49 | **Immutable, SIEM-exportable audit trail + administrative config-change auditing** (first item of the bank-readiness gap list). A new append-only, **hash-chained** `audit_log` table (migration 010 — each row `hash = sha256(prev_hash + canonical content)`, so tampering is detectable) backed by an `AuditLogRepo` that exposes only append + read (no update/delete — the app can't rewrite history). A central **`AuditService`** records events serialized (chain consistency), persists durably, and **exports each event off-box in real time**: a structured stdout line (`audit:true`, `schema:kravn.audit.v1`, for k8s→SIEM log shipping) and, when `observability.auditWebhookUrl` is set, a POST to a SIEM webhook (Splunk HEC / generic) — **SSRF-guarded** (`assertUrlAllowed` + `redirect:'error'` + 10s timeout) so the audit stream can't be pointed at an internal/metadata host. Secret material is **deep-redacted** (values whose key matches password/secret/token/apiKey/clientSecret/credential/… are blanked) and oversized values capped before anything is stored or exported. A global `onResponse` hook records **every mutating, authenticated control-plane `/api/*` call** (actor id/email/role, method+path, resource, outcome, IP, redacted body); high-volume data-plane (chat) and auth-token churn are excluded, and the hook never throws into the request. New read/verify API (`GET /api/audit`, `POST /api/audit/verify`) gated by a new **`audit.read`** permission (admin-only by default; grantable to a dedicated auditor role for segregation of duties); `verify()` recomputes the chain and reports the first break. Validated end-to-end on real sqlite (persistence, genesis→chain linkage, redaction of `clientSecret`/`password`, per-event stdout export, verify-ok, and verify **detecting a direct-DB tamper**). Documented limits for reviewers: the in-process chain is **per-replica** (the durable off-box SIEM export is the cross-replica tamper-proofing), and the table grows unbounded by design (retention is managed at the DB/SIEM layer, and true WORM is a deployment control). |
| v0.1.47 | **Fixed a startup race that logged `duplicate key value violates unique constraint "tools_pkey"` (PG 23505).** At boot, `syncPluginServers()` and `syncAll()` both fire un-awaited and can both trigger a catalog sync for the **same enabled** plugin server (e.g. a configured Jira). `syncCatalog` does delete-then-insert and is **not atomic**, so the two concurrent runs raced and the second's `INSERT` collided on the tool ids (the item was then skipped by the v0.1.36 per-item resilience — noisy warnings, non-fatal, catalog still correct). Catalog syncs are now **serialized per server** via a chained promise lock (`syncLocks`): runs for the same server queue behind each other so a delete always precedes its own inserts, while **different servers still sync in parallel**, a failed run doesn't wedge the chain, and the lock map self-cleans. Validated on the real method (serialized same-server, parallel different-server, no leak, error-not-wedged). No data or schema change. |
| v0.1.46 | **Brand consistency — the gold raven everywhere.** The raven mark in the operator console and the end-user client now uses the gold accent (`--accent`) instead of the theme `--brand` (which rendered dark/white), and the operator's favicons (`.svg` / `.png` / `.ico`) plus the client's favicon are unified to the gold-raven tile that the marketing site already uses. Visual/branding only — no behavior, auth, or data change. |
| v0.1.45 | **Renamed the internal `virtual server` identifiers to `MCP endpoint`** so the whole product matches the UI wording adopted in v0.1.38 (until now only the labels had changed). Renamed: frontend routes (`/virtual-servers` → `/mcp-endpoints`) + views, the `VirtualServer` type → `McpEndpoint`, the admin API (`/api/virtual-servers` → `/api/mcp-endpoints`), the consume-access helper + file (`vs-access.ts` → `endpoint-access.ts`), and the **RBAC permissions** (`virtualservers.*` → `endpoints.*`). **Backward-compatible by design:** (1) the consumer-facing MCP URL gains the canonical `/endpoints/:slug/mcp` while the old `/servers/:slug/mcp` keeps working as an **alias** — both routes share one handler (verified via Fastify inject), so **no connected client config breaks**; (2) the operator adds redirects from the old `/virtual-servers*` admin URLs. The DB table `virtual_servers` and column `virtual_server_id` are intentionally **UNCHANGED** (never user-visible; renaming them would need a 4-engine migration for no value). RBAC note: the permission strings changed, but they are a **static role→permission map derived at runtime (not persisted)**, so there is no data migration and every role keeps its same effective access. Mechanical rename verified by a full monorepo typecheck/build (6 projects green) and a per-form sweep (Pascal/camel/hyphen/permission) that preserves the snake_case DB names. |
| v0.1.44 | **Fix: creating a new MCP endpoint failed with "MCP endpoint not found".** The full-page endpoint editor decided create-vs-edit mode from a route param (`route.params.id === 'new'`), but the create route `virtual-servers/new` is a static path with **no `:id` param** — so `route.params.id` was `undefined`, the check never matched, and the view tried to load a non-existent endpoint (id `"undefined"`) and bounced back with a not-found toast. Now detected by route **name** (`virtual-server-new`, with an `!route.params.id` fallback). Pure operator-UI correctness fix — no backend, auth, or data-model change. |
| v0.1.43 | **Plugin detail modal + step-by-step setup docs; removed identifying example text.** Clicking a plugin in the marketplace now opens a **detail modal** with its full description, type/version/author, hook points, and a **"Setup & required permissions"** panel — the `setup` guidance was expanded into a numbered walkthrough for creating the Entra ID **app registration** (register → copy client/tenant id → client secret → add the read-only Graph Application permissions → grant admin consent) for the Teams and SharePoint plugins. Separately, an illustrative example in the Teams plugin's tool/description text and internal notes named real people; that text was made purely functional (no names), and — since it had already been committed — the git history was rewritten to purge it from the introducing commit's content and message, with the affected release tag moved to the scrubbed commit and force-pushed. Presentation/documentation change plus a privacy scrub; no change to plugin behavior, auth, or the data model. |
| v0.1.42 | **Native Microsoft Teams plugin** (app-only Microsoft Graph, read-only) + a `setup`/permissions field in the plugin marketplace. Like the SharePoint plugin, Teams is reimplemented in-process over Graph (client-credentials); 7 read-only tools: find people, find/list/read chats (scopable by an ISO `since`/`until` date window — e.g. "what did X and Y talk about yesterday"), and list teams/channels + read channel posts. Message HTML bodies are reduced to Markdown (the hardened shared `lib/html.ts`) for far fewer tokens. Hardening (built-in + adversarial-review-verified): the Graph host is FIXED (no caller-supplied base URL → no SSRF surface like the Jira plugin's baseUrl); every id is `encodeURIComponent`'d into the path; pagination follows only an `@odata.nextLink` that is itself on graph.microsoft.com AND `redirect: 'error'` refuses any off-Graph redirect, so the bearer token can never be exfiltrated to another host; per-request timeout; and message-count + 60 KB output caps bound the model context. The adversarial review found **no critical/high/medium issues**; its two low/info notes are fixed here AND back-ported to SharePoint: the app-only token cache key now includes a hash of the client secret (a config supplying a different/blank secret can never be handed a token minted from another config that shares the tenant+client id), and the transcript sort tolerates an unparseable timestamp. A new reusable **`setup`** manifest field (plugin-sdk + `PluginView`) renders a "Setup & required permissions" callout in the config screen — Teams and SharePoint now document exactly which Graph Application permissions (all read-only) the entered credential must be granted. `clientSecret` stays `secret: true` (encrypted at rest). Broad app-only Graph access stays gated by Kravn's data-plane entitlements (an admin composes these tools into a restricted MCP endpoint for authorized teams). Validated with a mocked-Graph harness: correct URLs + encoding, `$search` people-lookup with a quote-sanitised query + `ConsistencyLevel: eventual`, date-window filtering with early-stop (doesn't walk the whole history), secret-hash cache isolation, and the bearer token never leaving graph.microsoft.com (a malicious nextLink is rejected). |
| v0.1.41 | **Clearer authorization errors on denied MCP endpoints** (distinguishing *forbidden* from *unauthenticated*). When an MCP client (Claude, ChatGPT, Gemini, …) is denied a restricted endpoint because the user isn't in an allowed team, Kravn already returned a semantically-correct **403** — but with no structured signal, so clients collapsed it into a generic "authorization failed — check your credentials". The 403 now carries the **RFC 6750 `insufficient_scope`** challenge (`WWW-Authenticate: Bearer error="insufficient_scope", error_description="…"`) so a spec-compliant client can tell an **authorization** failure (valid token, missing permission) from an authentication one and surface the reason; the body/description is **actionable** (names the endpoint + "ask an admin to grant your team access"). Genuine **401** (no/invalid token) keeps the RFC 9728 `resource_metadata` challenge, unchanged. Every denial is now recorded to the admin **Logs view** (`event: mcp.access.denied` — actor id/email, their teams, the endpoint slug, the required `allowedTeams`) so an operator can diagnose an opaque client-side failure at a glance. **No new disclosure to the caller:** the client response never reveals *which* teams are allowed (that stays in the admin-only, `logs.read`-gated log); the `error_description` is quoted-string-sanitised so a crafted slug can't inject or break the header. The denial log fires only after successful authentication and lands in a bounded (500-entry) ring buffer — no unbounded growth. Validated via Fastify `inject` (admin outside the endpoint's team → 403 + `insufficient_scope` + log; no token → 401 unchanged; a `a"b\c` slug can't break the header). Presentation of the description remains client-side — Kravn now sends the correct, richest standards signal. |
| v0.1.40 | **DOCX → structured Markdown for token reduction** + a shared HTML module. The document extractor (`chat/extract.ts::extractText`, shared by the SharePoint plugin's `readDocument` and chat file uploads) now renders `.docx` via `mammoth.convertToHtml` → a reduced Markdown instead of flat `extractRawText`, preserving headings/lists/bold and — the real win — **tables**; far fewer tokens than the source XML and more legible than flat text (very large docs whose HTML exceeds the 128 KB processing cap fall back to plain-text extraction). `htmlToMarkdown` + its entity/tag guards were lifted verbatim from `native-hooks.ts` into a shared `lib/html.ts` (the HTML→Markdown and SafeHTML hooks now import it — behaviour-preserving refactor, helpers byte-identical) and gained `<table>` support. Adversarial review found + fixed three issues on this untrusted-content path: (1) mammoth inlines images as **base64 `data:` URIs** by default — a token bomb that also smuggles a large opaque payload into the model prompt → `convertImage` now drops images, and `htmlToMarkdown` drops `data:`/`javascript:`/`vbscript:` and >2 KB image srcs; (2) **MEDIUM — table-cell escaping was bypassable via HTML entities**: because the pipeline's final `decodeEntities` ran *after* per-cell escaping, a crafted DOCX/HTML cell (`&#124;` → `|`, `&#10;` → newline, incl. multi-encoded `&amp;#124;`) could inject **phantom columns** or **break out of the table** into fabricated headings / prompt-injection lines — defeating a control this change introduced. Fixed structurally: tables are rendered to a NUL-delimited placeholder and restored only *after* the final decode pass, with a fix-point entity-decode + tag-neutralise + pipe-escape per cell, so cell escaping is FINAL and holds at any encoding depth (verified single/double/triple `&amp;…`, hex `&#x7c;`, `&#10;` newline breakout — all contained); (3) **LOW — `imgSrc`'s `data:` filter** was bypassable via an entity-encoded scheme → now tests the fully-decoded src. ReDoS stays bounded (≤~270 ms on a 128 KB pathological table/entity input; >128 KB passes through unprocessed; fix-point decode is length-monotone). Validated end-to-end with a real jszip-built `.docx` (tables/headings/bold, no base64 leak, no phantom columns), the 9 native hooks still load, and SafeHTML still strips XSS. |
| v0.1.38 | **Decoupled the control plane from the data plane for MCP-endpoint (virtual-server) consumption.** Consuming an endpoint is now governed ONLY by its access policy — `public` / `authenticated` / `restricted → team membership` — via a single shared `canConsumeVirtualServer()` (`mcp/vs-access.ts`). **Removed** the platform-role axis (`allowedRoles`) and the implicit **admin bypass** from consumption: being a Kravn config-admin no longer grants automatic access to a restricted endpoint — an admin consumes it by being in one of its teams, exactly like anyone else (the two planes are now orthogonal; one identity can be both). The same helper is used at ALL THREE enforcement points — the MCP endpoint (`mcp.routes`), the chat options list, and the chat tool-resolution (`chat.service`) — fixing a prior inconsistency where the MCP path had an admin bypass but the chat path did not. The change is strictly MORE restrictive (it can only deny, never open); `mcp.invoke` (held by every role) remains the baseline "is a consumer" capability, the per-team tool grant still narrows WHICH tools after access, and the global `/mcp` catalog stays admin-only. Validated 6/6: an admin not in the team is denied a restricted endpoint (MCP + chat), an authenticated endpoint is allowed, and adding the admin to the team restores access. **Upgrade note:** restricted endpoints that relied on `allowedRoles` or the admin bypass now need the right teams set. |
| v0.1.36 | **Catalog sync hardened against oversized/unbounded upstream values.** An upstream (or plugin) MCP server can return arbitrary tool/resource/prompt metadata; a value longer than a bounded column (a `name`/`mime_type` `varchar`, or any `TEXT` on MySQL where TEXT caps at 64 KB) made the DB driver reject the row and — because one throw aborted the whole loop — **failed the entire catalog** ("Connected, but the server's catalog could not be stored"). `syncCatalog` now clamps bounded fields (`name`→255, `mime_type`→191, text→60 KB, oversized JSON schema→`{}`) and stores each item independently (a single item that still can't be stored is skipped + logged, not fatal), so a mostly-good catalog lands and one hostile/huge value can't deny the whole connection. Raw DB detail still goes to the logs; the UI keeps the generalized message (no SQL/column leak). |
| v0.1.35 | **Opt-in pipelines + per-VS pipeline in the VS editor.** The global pipeline is now **opt-in** (like a VS overlay): a plugin runs only where the admin explicitly adds it — nothing is auto-seeded, no legacy priority fallback. `enabledHooks` = the added-and-enabled global steps, then the added-and-enabled VS-overlay steps. This is strictly **safer** (nothing runs unless deliberately composed) and the compliance invariant is preserved + re-validated (7/7): the global base always runs first for a VS call and a VS overlay still cannot remove/bypass it (global steps are inherited read-only in the VS editor). Migration 009 clears the previously auto-seeded global rows once (per-VS overlays preserved). Frontend is presentation: a reusable `PipelineEditor(scope)` component, the VS create/edit moved from a modal to a full page that embeds it; pipeline mutations remain settings.write-gated regardless of where the component is shown. Self-reviewed (simplification of already-reviewed threading/authz; no change to how `virtualServerId` is derived or gated). |
| v0.1.34 | **Three compliance hook plugins** (native, disabled by default) + marketplace Plugins UI. **Prompt-Injection Guard** — detects indirect prompt injection in results ("ignore previous instructions", role-tag spoofing, exfiltration directives) and redacts/annotates/fences them (the #1 MCP-specific risk). **Audit / Compliance Logger** — writes a tamper-evident **hash-chained** audit record (actor, VS, tool, clipped/redacted arg+result preview, timestamp) per tool call to the Logs view (durable store + SIEM export are a follow-up). **PII Tokenizer** — replaces emails/IPs/credit-cards(Luhn)/phones with stable deterministic tokens (`⟦EMAIL_ab12⟧` = sha256(salt|type|value) truncated, not reversible without a map) so the model reasons consistently without seeing the value; reversible restore-to-user is a follow-up. Adversarial review found + fixed: a **quadratic ReDoS** in the injection guard's "disregard" pattern (two variable-whitespace matchers around optional groups — `"disregard "+" "×200k` stalled the event loop; the try/catch can't catch a hang) → anchored so each optional carries its own `\s+` (now 28 ms via the pipeline, was ~9 s+); the PII default salt was the public string `'kravn'` (brute-forceable low-entropy PII) → now derived from the deployment secret (stable + shared across replicas) with a per-process random fallback, token widened 6→10 hex; the audit logger's `JSON.stringify` wrapped so a non-serializable value can't silently drop the very call it audits. The other 10 injection patterns + all PII regexes confirmed linear (bounded quantifiers, anchored keywords). Each hook is inside the pipeline per-step try/catch. Marketplace UI is presentation-only (search + type/hook-point filters + Catalog/Installed), still settings.read/write gated. |
| v0.1.33 | **Six built-in content hook plugins** (`native-hooks.ts`, disabled by default): Secrets Redactor (strips private keys/API tokens/JWTs/credentials from results), Content Safety Filter (lexicon-based self-harm/violence/hate redaction + optional request block), Deny List Filter (block requests / redact results by phrase or regex), HTML→Markdown, SafeHTML Sanitizer (strip script/style/iframe/on\*/js: URLs), TOON Encoder (compact result re-encoding). They run in-process (same trust model as the seeded tool-guard) but as **native HOOK plugins** — a small manager change makes `native` a `KravnPlugin` map and resolves hooks via `pluginFor()` (native ∪ loaded); native hooks seed **disabled** (opt-in, since they change behaviour) while native mcp-servers keep auto-enable. Regexes are written linear (bounded quantifiers, lazy tag bodies) to avoid ReDoS on untrusted upstream content; each hook is wrapped by the pipeline's per-step try/catch so a bad transform degrades to "skipped", never a crash. Adversarial review (empirical) found + fixed: a **quadratic ReDoS** in the two HTML plugins (an upstream returning 640 KB of unclosed `<iframe>` blocked the event loop ~4.6 s) → tag strips use `[^<>]` (linear on `<`-runs) + a 128 KB input cap (a 320 KB/160 KB "bomb" now completes in <1.5 s total); a **decode-after-strip** that could reconstruct live markup from entities → decode then neutralise; several SafeHTML bypasses (`<svg/onload>`, unquoted `javascript:`) hardened; `decodeEntities` clamps out-of-range code points (was throwing → silently disabling conversion); `hookPointsOf` resolves native hooks (Plugins screen was blank for them); high-entropy secret mode skips pure base64/data-URI blobs (was corrupting binary). **Residual (documented in the plugin):** SafeHTML is best-effort regex sanitisation, NOT a substitute for a DOM sanitizer at the render layer; Content Safety is lexicon-based, not a classifier; operator-supplied regexes run on untrusted content (no engine timeout). All opt-in / disabled by default. |
| v0.1.31 | **Per-virtual-server pipeline overlays.** Hook chains are now composed per scope: a mandatory GLOBAL base (runs for all traffic) + opt-in per-VS overlays (`pipeline_steps.scope`, migration 008) that run, in addition to global, only for calls routed through that virtual server. The `virtualServerId` is threaded server-side from `buildScope` (the resolved, entitled VS) → `dispatch` → `invokeTool`/`readResourceFrom`/`getPromptFrom` → the hook ctx; it is NOT caller-supplied. **Invariant (compliance): a VS overlay can only ADD steps — it can never disable, reorder-away, or bypass a global step.** `enabledHooks()` always runs the full global chain first, then the VS overlay; `setPipeline(scope,…)` for a VS writes only VS-scoped rows and cannot touch global rows; auth (`onResolveUser`) is global-only and rejected as a VS overlay. Routes gain a `:scope` path param under the same settings.read/settings.write admin gate; scope + hook point are validated (global or an existing VS) before any DB/exec, all SQL parameterized. VS deletion drops its overlay (`deleteByScope`). Validated 19/19: a plugin disabled globally but added to VS-A's overlay runs ONLY for VS-A (not VS-B, not global). |
| v0.1.30 | **Hook Pipelines** — compose, per MCP lifecycle junction (`onToolCall`/`onToolResult`/`onList*`/`onResource*`/`onPrompt*`/`onResolveUser`), the ORDERED chain of hook plugins + a per-junction on/off, with a trace dry-run (`cluster`-agnostic; `pipeline_steps` table, migration 007). `enabledHooks()` now runs plugins in the DB-defined per-hook order (falling back to global `priority` only before steps are seeded); a hook still runs **only if the plugin is globally enabled AND its step is enabled at that junction**, so nothing runs that a `disabled` plugin's master switch turned off. New routes `GET /api/pipeline` (settings.read) + `PUT /api/pipeline/:hookPoint` + `POST /api/pipeline/:hookPoint/trace` (settings.write) — same admin gate as the Plugins screen; the `:hookPoint` is validated against the known hook set before any DB/exec use, and all SQL is parameterized. `setPipeline` rejects plugins that don't implement the hook, dedupes, and caps steps (≤200). Trace runs the real (enabled) steps on admin-supplied synthetic JSON, per-step errors/deny contained, payload never persisted. Ordering/toggle only reachable by admins (settings.write), so it cannot weaken a control for a non-admin. Adversarial review (no non-admin/unauth exploit, no silent weakening, all SQL parameterized) found + fixed two admin-only rough edges: the `:hookPoint` guards now use `Object.hasOwn`-style checks (inherited keys like `constructor`/`__proto__` no longer pass, though their effect was already inert); and a partial/empty `PUT` now records omitted implementers **present-but-off** and `enabledHooks` only falls back to global priority when *nothing* is seeded — so clearing a junction runs nothing (and the view matches) instead of resurrecting every plugin. |
| v0.1.29 | **Multi-replica prep — cross-replica shared store** (`cluster/shared-store.ts`, opt-in via `KRAVN_REDIS_URL`; Helm deploys **Dragonfly**, RESP-compatible). The brute-force **rate-limit** counters (login + OAuth DCR, `LoginRateLimiter` now async + namespaced) and **in-flight OIDC login state** (PKCE verifier + `state`, previously per-process Maps) move into the shared store, so both are correct behind N pods (validated: a lockout raised on pod A blocks pod B; the counter lives in Dragonfly with the right TTL). **Fail-safe by construction:** a store outage degrades each pod to its own in-process counters (per-pod limiting — never fail-open, never a crash; an unreachable URL only logs + degrades), and the in-process memory-safety hardening (bounded maps, eviction that never wipes an active block) is preserved in the memory backend. Review found + fixed a regression it introduced: the OIDC `state` claim was `get`+`await`+`del` (a double-use window across replicas) → replaced with an **atomic single-use** `take()` (`GETDEL` / memory get-and-delete), verified single-winner under concurrency. Helm Dragonfly pinned to `--proactor_threads=1` (it refuses to boot unless `maxmemory ≥ threads×256MiB`), ClusterIP-only, non-root. No behavior change when `KRAVN_REDIS_URL` is unset. |

Keep this table current: every release that touches security adds a row.
