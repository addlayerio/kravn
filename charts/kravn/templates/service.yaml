apiVersion: v1
kind: Service
metadata:
  name: {{ include "kravn.fullname" . }}
  labels:
    {{- include "kravn.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "kravn.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
